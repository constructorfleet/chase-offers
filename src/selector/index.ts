import * as account from "./account.selectors";
import * as auth from "./auth.selectors";
import * as offers from "./offers.selectors";
import * as preAuth from "./pre-auth.selectors";

export const selectors = {
  preAuth,
  auth,
  account,
  offers,
};
